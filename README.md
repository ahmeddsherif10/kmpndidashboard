
  # Apply Direction 1 Only

  This is a code bundle for Apply Direction 1 Only. The original project is available at https://www.figma.com/design/JEty68KNTXpl0gzsxzmMQz/Apply-Direction-1-Only.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  